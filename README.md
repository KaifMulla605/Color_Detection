# Color_Detection
This Python-based project implements an interactive color detection system using OpenCV, NumPy, and Pandas. Users can click on an image to identify the pixel's RGB values and determine the closest matching color from a dataset using Manhattan distance
